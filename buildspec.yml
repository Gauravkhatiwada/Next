version: 0.2

phases:
  install:
    commands:
      - npm ci

  build:
    commands:
      # Consider adding a linting step to catch errors before building
      - sudo npm run build

artifacts:
  files:
    - '**/*'
  base-directory: "./"
